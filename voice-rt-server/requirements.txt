fastapi==0.115.0
uvicorn[standard]==0.32.0
numpy==1.26.4
websockets==13.1
soundfile==0.12.1

# infer_rvc_python (MIT license, R3gm) — the actual RVC inference library
# convert_chunk() in server.py is built on. Needs ffmpeg installed on the
# system binary: apt-get install -y ffmpeg (install-deps.sh does this).
# ffmpeg-python alone is not enough. fairseq (an infer_rvc_python dependency)
# isn't yet compatible
# with Python 3.11+ as of writing — if you hit a fairseq install error, run:
#   bash install-deps.sh
# on the RunPod pod instead of plain `pip install -r requirements.txt`.
infer-rvc-python==1.2.0

# Torch is pulled in by infer-rvc-python, but pin explicitly so you control
# which CUDA build gets installed rather than getting whatever the resolver
# picks. Match this to your RunPod pod's actual CUDA version — check with
# `nvidia-smi` on the pod once it's running.
torch==2.4.0
torchaudio==2.4.0